import { CoinHoarder } from '../ts/CoinHoarder';
import { expect } from 'chai';
import 'mocha';

describe('Hello function', () => {

  it('should return hello world', () => {
    const result = "Hello world!";//hello();
    expect(result).to.equal('Hello world!');
  });

});